using System;
using System.Collections.Generic;
using System.Text;

namespace FInanceSupport
{
    class ID
    {
        public static List<int> id = new List<int>();

        public static void OutputListId()
        {
            int index = 1;

            for (int i = -1; i < Income.index || i < Expenses.index; i++)
            {
                id.Add(index);

                index++;
            }
        }
    }
}
